#include <stdio.h>

int main() {
  int t, n, i, j, sum = 0;

  scanf("%d", &t);

  while (t--) {
    scanf("%d", &n);

    int ar[n];
    for (int i = 1; i <= n; i++)
    {
        printf("%d ", ar[i]);
    }
    
  }

  return 0;
}
