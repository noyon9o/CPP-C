#include<stdio.h>
#include<string.h>
int main()
{
    int t, n, r;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        scanf("%d %d", &n, &r);
        char s[r];
        while (n--)
        {
            scanf("%s", s);
        }
        
       }
    return 0;
}